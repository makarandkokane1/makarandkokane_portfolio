PROJECT (sampleWin32VTKProj1)

IF(NOT VTK_BINARY_DIR)
FIND_PACKAGE(VTK)
IF(NOT VTK_DIR)
  MESSAGE(FATAL_ERROR "Please set VTK_DIR.")
ENDIF()
INCLUDE(${VTK_USE_FILE})
ENDIF()

LINK_LIBRARIES(
  ${VTK_LIBRARIES}
)

ADD_EXECUTABLE(sampleWin32VTKProj1 WIN32 sampleWin32VTKProj1.cxx)
